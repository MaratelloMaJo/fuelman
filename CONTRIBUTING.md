# Вклад в проект FuelMan / Contributing to FuelMan

- Создавайте ветки от `main` в формате: `feat/название-фичи` или `fix/название-бага`.
- Прогоняйте `flutter analyze` и `flutter test` перед отправкой PR.
- Не хардкодьте строки: все тексты выносите в `lib/l10n/` (ru, en, kk).